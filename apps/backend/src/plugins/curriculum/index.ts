/**
 * Curriculum Plugin — Registration Order: Position 3 (domain plugin)
 *
 * Request decorator pattern: fastify.decorateRequest('uid', '')
 * Serves milestone content, briefs, acceptance criteria.
 *
 * Actual implementation in Story 4.x.
 */

export {}
